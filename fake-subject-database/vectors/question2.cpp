#include "part.hpp"

// Add code in part.hpp so that the following works

int main(int argc, char* argv[]) {
  std::vector<Foo> l;

  l.push_back(Foo(1)); // Implement what is required in part.cpp

  return 0;
}
